# Notebooks

Optional exploration notebooks only (04-python-discipline.md §5). Never use
notebooks for the shipped pipeline - the pipeline lives in `src/radar/` and
runs via `uv run radar ...`. Keep notebooks exploratory, seeded, and out of
the shipped code path.